# econ115B

React + Vite starter project. Production builds are written to `docs/` for GitHub Pages hosting.

```bash
npm install
npm run dev
```

Create a production build:

```bash
npm run build
```
